{
    "version": 2,
    "builds": [
        {
            "src": "api/**/*.js",
            "use": "@vercel/node"
        },
        {
            "src": "package.json",
            "use": "@vercel/node",
            "config": {
                "command": "npm run generate && npm run build"
            }
        }
    ]
}